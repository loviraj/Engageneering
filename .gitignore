# ── SECRETS — NEVER COMMIT ────────────────────────────────────────
config.js
.env
*.env
!.env.example
*.pem
*.key
*.p12
*.pfx
service-account*.json

# ── PYTHON ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.so
.mypy_cache/
.ruff_cache/

# ── VIRTUAL ENVIRONMENTS ──────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── NODE ──────────────────────────────────────────────────────────
node_modules/
package-lock.json
.npm/

# ── IDE ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
Thumbs.db
desktop.ini

# ── LOGS & TEST ARTIFACTS ─────────────────────────────────────────
*.log
logs/
.pytest_cache/
htmlcov/
.coverage

# Local Netlify folder
.netlify

# ── LARGE BINARIES — use CLI from PATH instead (M-4) ─────────────
# supabase.exe is 98MB and must not be committed to the repo
supabase.exe
supabase

# ── BACKUP / DEBUG HTML FILES — not for deployment (M-5) ─────────
# These old/working/debug files are published by Netlify (publish = ".")
# and may contain older, less secure auth logic or debug output.
*OLD.html
*OLD1.html
*OLD-WKG.html
*Working.html
whats_broken.html
vai2bebo*.html
